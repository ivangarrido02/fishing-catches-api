spring:
  application:
    name: registrocapturas

  datasource:
    url: jdbc:postgresql://localhost:5432/registrocapturas
    username: postgres
    password: cap123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100